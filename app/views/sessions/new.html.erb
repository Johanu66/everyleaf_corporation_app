
<div class="containercontent_main justify-content-center align-items-center d-flex flex-column jus" style="height:90vh;">
    <h1 class="text-center">Connexion</h1>
    <div class="row">
        <div class="col-12 col-md-6 d-flex flex-column justify-content-center align-items-center">
            <%= form_with(model: @user, scope: :session, local: true,url: create_session_path, class: " border p-3 form text-center") do |form| %>

                <div class="field my-3">
                    <%= form.text_field :email, placeholder: "Adresse email" %>
                </div>

                <div class="field my-3">
                    <%= form.password_field :password, placeholder: "Mot de passe" %>
                </div>

                <div class="actions my-3">
                    <%= form.submit "Se connecter", class: "btn btn-primary" %>
                </div>
                <hr>
                <%= link_to "Créer un compte", new_user_path, class: "btn btn-primary text-white my-3 yet_a_compte" %>
            <% end %>
        </div>
    </div>
</div>